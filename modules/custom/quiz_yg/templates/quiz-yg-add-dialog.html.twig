{#
/**
 * @file
 * Default theme implementation of modal add paragraph dialog template.
 *
 * Following classes have custom use:
 *   - quiz_yg-add-dialog - is used to wrap the dialog.
 *   - quiz_yg-add-dialog-row - is used to wrap the paragraph type rows.
 *
 *
 * @ingroup themeable
 */
#}
{{ add }}
<div class="quiz_yg-add-dialog js-hide">
    <ul class="quiz_yg-add-dialog-list">
    {% for button in buttons %}
      <li class="quiz_yg-add-dialog-row">
        {{ button }}
      </li>
    {% endfor %}
  </ul>
</div>
